
import { createAction } from '@reduxjs/toolkit';

export const setLetterCounter = createAction('counter/setLetterCounter');
export const setFirstWordTitle = createAction('counter/setFirstWordTitle');

